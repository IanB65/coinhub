# CoinHub — New VM / New Machine Handover
**Generated:** 23 March 2026
**Purpose:** Complete handover to continue this project in Claude Code on a fresh machine, picking up exactly where we left off.

---

## PART 1 — FILES TO COPY

Copy this entire folder to the new machine. Every file matters:

```
CoinHub.html                        ← THE APP (~415 KB, self-contained website)
auth_server.py                      ← Python auth + file server (no pip deps needed)
.coinhub_auth                       ← Hashed login credentials (email + TOTP)
.coinhub_config                     ← Notion API token + database IDs
.coinhub_sync_queue.json            ← Pending Notion sync items (see §SYNC QUEUE)
CoinHub_ClaudeCode_Handoff.md       ← Architecture reference doc
CoinHub_Migration_Handover.md       ← Previous migration doc (still valid)
CoinHub_NewVM_Handover.md           ← This document
```

**Do NOT copy:**
- `.claude/` folder — Claude Code local config, machine-specific, do not port
- `CoinHub.xlsx` — Excel export, not used by the app

---

## PART 2 — SYNC QUEUE STATE (ACTION REQUIRED)

The `.coinhub_sync_queue.json` currently contains **6 stale `delete_variant` entries with empty `variantCode` fields** — these are invalid/unprocessable.

**At the start of the first session on the new machine: clear the queue to `[]`.**

```json
[]
```

These were generated by the UI on 20-22 March 2026 and cannot be actioned (no variantCode). No Notion changes are pending.

---

## PART 3 — RUNNING THE SERVER ON THE NEW MACHINE

### Requirements
- Python 3.9+ (stdlib only — no pip installs)
- Port 8090 available

### Start
```bash
python auth_server.py
```

Open: `http://localhost:8090`

Login credentials are already in `.coinhub_auth` — **no re-setup needed**:
- Email: `ian@ghghome.co.uk`
- Password: same as before (hashed in file)
- TOTP: enabled — use the same authenticator app (same TOTP secret carries over)

### LAN access (optional)
Edit `auth_server.py` line ~322, change `'localhost'` → `''` (empty string = all interfaces).

---

## PART 4 — CLAUDE CODE SETUP ON NEW MACHINE

### Step 1 — Open the project folder in Claude Code

Set the working directory to wherever you copied the files.
When Claude Code opens, it will see `CoinHub.html`, `auth_server.py`, etc.

### Step 2 — Install the Notion MCP

The Notion MCP must be connected for Claude to read/write Ian's collection database.

In Claude Code settings → MCP servers, add the Notion integration.
The API token is: (stored in `.coinhub_config` — see §CREDENTIALS below)

The MCP must be connected to **Ian's second Notion account** (not his primary).

### Step 3 — Install the Coin Collection Skill

The `coin-collection` skill must be installed in Claude Code (via the skills plugin).
The skill file content is documented in the existing session — the skill covers:
- Finding new Royal Mint coins
- Looking up coin images (6 approved sources in priority order)
- Checking Ian's Notion collection database
- Coin values and mintage figures

Trigger phrases that activate the skill automatically:
- Any mention of coins, Royal Mint, coin collection, 50p, £2, commemorative, proof, BU, numismatics
- "do I have this coin", "find me an image of...", "what's new from the Royal Mint", etc.

### Step 4 — Verify the MCP connection

Run a quick `notion-search` for "coin collection" to confirm the MCP connects to the right workspace.

---

## PART 5 — CREDENTIALS & CONFIG

### `.coinhub_auth` (login credentials — copy the file)
```json
{
  "email": "ian@ghghome.co.uk",
  "pw_hash": "<PW_HASH_REDACTED>",
  "totp_secret": "<TOTP_SECRET_REDACTED>",
  "totp_enabled": true
}
```

### `.coinhub_config` (Notion API — copy the file)
```json
{
  "notion_token": "<NOTION_TOKEN_REDACTED>",
  "notion_databases": {
    "variant":           "1bf05769-e1ee-81c7-81dc-000b9d014020",
    "instance":          "1a605769-e1ee-80d2-b868-000b80373e62",
    "storage_container": "1d705769-e1ee-80e8-8821-000b783319c7",
    "storage_page":      "1d805769-e1ee-807a-9b01-000b22c54e0c",
    "storage_slot":      "1d805769-e1ee-801e-b1a4-000b9de0b849",
    "location":          "1d805769-e1ee-8042-b4dd-000b1393168e"
  },
  "notion_parent_page": "1a505769-e1ee-806a-883d-c8df0a47b311"
}
```

### Key Notion page IDs
```
Workspace parent page:  1a505769-e1ee-806a-883d-c8df0a47b311
CoinHub Inbox page:     32205769-e1ee-818a-8ea8-e45f1efcbdca
Recovery/context page:  32105769-e1ee-81f3-af73-d74c48d5e86b
```

---

## PART 6 — PROJECT STATE AS OF 23 MARCH 2026

### What CoinHub is
A single-file HTML/CSS/JS gallery website for Ian's UK coin collection. No framework, no build step, no database engine. Served locally via `auth_server.py`. All data is embedded in `CoinHub.html`.

### Data snapshot
| Metric | Count |
|---|---|
| RAW coin variants | 1,430+ |
| COTUK_MAP image entries | 646 |
| Physical instance records | 1,496 |
| Variant codes with instances | 676 |
| Collections | 25 |

### Collections status
| Collection | Got | Total | Notes |
|---|---|---|---|
| Pre Decimal | ~454 | 1,095 | |
| London 2012 Olympics 50p | 30 | 30 | Complete |
| Beatrix Potter | 15 | 15 | Complete |
| Winnie the Pooh | 9 | 9 | Complete |
| Harry Potter | 8 | 8 | Incl Winged Keys 2024, Flying Car 2025 |
| Star Wars | 8 | 8 | Incl X-Wing, Death Star II 2024 |
| Snowman | 8 | 8 | Complete |
| Dinosaurs | 12 | 12 | Complete (2020×3, 2021×3, 2024×6) |
| Definitives (KCIII) | 27 | 35 | |
| Shield | 8 | 16 | Only 8 Got — do NOT change this |
| 10p Alphabet | 0 | 52 | None owned — do NOT change this |
| Paddington Bear | 4 | 4 | Complete |
| 2007 Proof Set | 12 | 12 | Complete |
| Britannia | 13 | 43 | |
| Military | 13 | 14 | VE Day 2025 = Need |
| UK Anniversary | 24 | 27 | |
| Science | 6 | 6 | Complete |
| Peter Pan | 6 | 6 | Complete (Isle of Man coins) |
| Characters | 5 | 6 | Gruffalo & Mouse 2019 = Need |
| Sports | 5 | 8 | |
| EU | 2 | 5 | |
| Commemorative | 4 | 5 | Incl Concorde 2026 Got |
| Disney | 1 | 1 | Mary Poppins 2025 |
| Monopoly | 1 | 1 | Monopoly 2025 Got |
| Old Size 50p | 0 | 1 | EEC 1992 — no instance |

### 2026 coins (already in RAW + COTUK_MAP, Status=List)
| Variant Code | Description |
|---|---|
| `UK-COMM-50P-2026-BRGP-` | Grand Prix Centenary 50p |
| `UK-COMM-50P-2026-KTRU-` | King's Trust 50th Anniversary 50p |
| `UK-COMM-£2-2026-ZSLL-` | ZSL London Zoo 200th Anniversary £2 (DOUBLE-L in code) |
| `UK-COMM-£2-2026-BEAG-` | HMS Beagle 200th Anniversary £2 |
| `UK-COMM-£5-2026-QEII-` | QEII 100th Birthday £5 |
| `UK-D-50P-2026-WPKD-` | Winnie the Pooh — Kindness 50p |
| `UK-D-50P-2026-DENN-` | Dennis the Menace 50p |

---

## PART 7 — ARCHITECTURE QUICK REFERENCE

### Critical data rules (bugs previously fixed — do not reintroduce)

```js
// ✅ Always reference globals directly — NEVER via window.
const url = COTUK_MAP[c.variantCode];          // NOT window.COTUK_MAP[...]
const insts = INSTANCE_DATA[coin.variantCode]; // NOT INSTANCE_DATA[coin.id]

// ✅ coin.variantCode is the key — coin.id does NOT EXIST
// ✅ INSTANCE_DATA must be at script level (global) — NEVER inside a function
// ✅ onerror="" attributes must be single-line — never multi-line
```

### RAW array format
```js
// [variantCode, name, denom, collection, monarch, year, status, imgUrl, instCount]
['UK-PD-FART-1902-', 'Farthing 1902', 'Farthing', 'Pre Decimal', 'King Edward VII', 1902, 'Got', '', 1]
```

### Monarch names (exact from Notion — do not "fix")
- `'King George Vi'` ← lowercase 'i' — intentional, matches Notion

### Design tokens
```css
--ink: #0C0C0A  --paper: #F5F2EB  --paper2: #EDE9DF  --paper3: #E4DFD2
--gold: #8B6914  --gold2: #C49A2A  --gold3: #F0C84A
--got: #2D6  --need: #E55  --list: #8B6914
```
Fonts: Bodoni Moda (headings), Cormorant Garamond (body), DM Mono (codes)

### Notion record creation — 3-step workaround (always use this pattern)
```
Step 1: notion-create-pages  →  parent: page_id "1a505769-e1ee-806a-883d-c8df0a47b311"
Step 2: notion-move-pages    →  new_parent: data_source_id "<target DB>"
Step 3: notion-update-page   →  set all properties
```
Direct creation to `data_source_id` fails with "Property ID not found".

### Notion API rules
- Always use **Ian's second email account** (not primary)
- `update_properties` and `replace_content` are **two separate calls**
- Pages outside databases: only `title` property allowed
- Bare UUID for page IDs — `collection://` prefix only for `data_source_id` in move operations

---

## PART 8 — WORKFLOWS

### Session start checklist
1. Read `.coinhub_sync_queue.json`
2. If it has items (and they have valid variantCodes), process via Notion MCP
3. Write `[]` back to clear the queue

### CoinHub Inbox workflow
Trigger phrase: **"Please process the CoinHub Inbox page"**
Notion URL: https://www.notion.so/32205769e1ee818a8ea8e45f1efcbdca

Two sections:
- **New Coins Spotted** → Claude creates Coin Variant records (Status=List), moves to Processed
- **I've Got It** → Claude creates Instance records + sets Variant to Got, moves to Processed

### Coin collection skill image sources (priority order)
1. Royal Mint — royalmint.com
2. Westminster Collection — westminstercollection.co.uk (UK coins only)
3. Coins of the UK — coins-of-the-uk.co.uk
4. UK Coin Hunt — ukcoinhunt.com
5. Coin Checker — coinchecker.co.uk
6. Fifty Pence — fiftypence.co.uk

---

## PART 9 — PENDING FEATURES (pick up from here)

### High priority
- [ ] Collection progress bars — % Got per collection, in sidebar or dedicated view
- [ ] Collection overview page — card grid: each collection, Got/Total, representative image

### Medium priority
- [ ] Mobile responsive improvements
- [ ] Export to CSV (current filtered view)
- [ ] 4 coins with collection="Unknown" — need classifying
- [ ] D-Day 1994 50p image URL verification

### Future ideas
- [ ] Statistics dashboard (charts: Got by denomination/decade/collection)
- [ ] Condition histogram
- [ ] Want list print view (for coin fairs)
- [ ] Value tracking
- [ ] Pre-decimal year range slider
- [ ] Image lightbox
- [ ] Variant code copy button

---

## PART 10 — WINDOWS / LAUNCH NOTES

- Run server with `python auth_server.py` (not `python3` — Windows uses `python`)
- `launch.json` for VS Code debug needs `"runtimeExecutable": "python"` and explicit `--directory` arg
- File is large (~415 KB) — when making changes, target specific sections only
  - UI/logic: last ~12 KB of the `<script>` block
  - New coin: find `// ── COLLECTION NAME ──` section in RAW, insert new row
  - New instance: add to `INSTANCE_DATA` object
  - New image: add to `COTUK_MAP` object

---

*This document was generated 23 March 2026 to enable a complete handover to a new VM. All credentials, config, architecture, and project state are captured above.*
